# TypeMaster 产品迭代记录

## 2026-06-09 结果页下一轮处方

本轮诊断：TypeMaster 已经具备评估、计划、挑战、练习、复盘的主闭环，但结果页的用户价值仍偏向“看分数和建议”。当 AI 建议加载中、失败或信息较长时，用户缺少一个足够短、可立即执行的下一轮目标。

本轮需求：在结果页新增确定性的下一轮练习处方，把本轮表现转化为“重点、剂量、验收线”三项可执行信息。

目标用户：完成一轮练习后想马上继续进步的打字训练用户，尤其是还没有形成稳定训练习惯的新用户。

使用场景：用户结束练习后进入结果页，不需要重新选择模式，也不必等待 AI 建议，就能知道下一轮该保护准确率、抹平节奏、减少返工，还是小幅加速。

预期收益：降低复盘后的决策成本，让练习闭环从“完成一轮”自然推进到“带目标再练一轮”，提升连续训练率和产品可信度。

改动范围：结果页模型、结果页 UI、双语文案、响应式样式和结果页相关测试。

验收标准：

- 结果页始终显示下一轮处方。
- 处方基于准确率、稳定性、原始速度与净速度差等本轮指标确定重点。
- 处方包含重点、练习剂量和验收线三项信息。
- AI 建议不可用时处方仍可工作。
- 桌面和移动端布局不挤压现有结果页操作。
- 已补充模型测试和页面渲染测试。

## 下一轮高价值方向

1. 练习前目标承接：把结果页处方带入下一轮练习页，让用户在输入前能看到本轮目标和验收线。
2. 错误热点训练文本：根据高频错字、错词自动生成短文本，让“减少返工”不只停留在提示层。
3. 计划完成后的再评估入口：当 7 天起步计划完成后，引导用户重新评估并生成下一阶段计划。
4. 挑战与计划的节奏保护：当挑战连续拉低准确率时，在首页和挑战页主动建议退出排名压力。
5. 数据导入后的恢复路径：导入历史数据后自动生成当前状态摘要，帮助跨设备用户快速续接训练。
