# TypeMaster 产品迭代记录

## 2026-06-09 计划完成后的再评估入口

本轮诊断：7 天起步计划完成后，系统已经知道计划状态是 `complete`，但用户路径仍然停留在“回首页看看”这类弱提示上。结果页、首页和计划页没有把用户自然带进新的评估流程，反而可能继续沿用旧画像或停在已经结束的计划里。

本轮需求：当起步计划完成后，明确引导用户重新评估，并基于新的评估样本生成下一阶段计划，让“完成一轮计划”自然过渡到“开始下一阶段训练”，而不是让用户自己决定下一步该做什么。

目标用户：已经完成 7 天起步计划的连续训练用户，尤其是愿意长期训练、希望系统持续给出下一阶段方向的用户。

使用场景：用户完成计划最后一轮后进入结果页，或稍后回到首页、计划页；系统不再把主动作留在“继续计划”或“回首页”，而是统一切换为“开始再评估”。点击后直接进入新的 3 轮诊断流程，完成后由系统生成下一阶段计划。

预期收益：让计划闭环不在“完成”处断掉，而是自动接到下一次成长评估；避免用户在旧画像上继续练，也避免完成计划后失去方向。

产品与技术设计：`buildResultDecisionModel` 和 `buildHomeDecisionModel` 新增完成态分支，把主动作改成再评估；`TrainingPlanPage` 在计划完成时切换为再评估说明和 CTA；训练流 use case 收住旧的“完成计划后继续沿用旧画像重建计划”路径，转而重启诊断流程。

改动范围：训练决策模型、训练流 use case、计划页 model/UI、双语文案、结果页与计划页测试、产品记录。

验收标准：

- 计划完成后，结果页主动作改为“开始再评估”。
- 首页主推荐在计划完成后改为再评估，而不是继续计划。
- 计划页在计划完成后显示再评估说明和入口。
- 点击再评估入口后直接进入新的诊断练习，而不是根据旧画像重建计划。
- 原有活跃计划、诊断中、挑战中、自由练习路径不回退。

## 2026-06-09 命中反馈沉淀到洞察页

本轮诊断：结果页已经能告诉用户某一轮自适应训练或键位专项有没有打到点上，但这种反馈仍然停留在单轮结束时。用户回到洞察页后，依然只能看到错误热点和当前压力，无法判断这些针对性练习是不是正在持续起作用。

本轮需求：把 adaptive / 键位专项的命中反馈沉淀到洞察页，形成一个长期趋势面板，让用户能看见最近几次专项训练是否在持续给问题降温，而不是只在某一轮结果页里短暂出现。

目标用户：已经进入连续训练状态、会反复使用“再练一轮”或键位专项入口的用户，尤其是希望判断训练建议是否真的有效、决定要不要继续窄练的进阶练习者。

使用场景：用户进入洞察页复盘最近表现；除了会话趋势、技能画像和键位压力，还能看到“命中趋势”面板，知道最近做了多少次专项回合、其中多少次真的让问题降温、当前哪些目标仍在持续出现。

预期收益：把结果页的单轮反馈升级成长期记忆，帮助用户建立“专项训练正在起作用”的可感知证据，也为后续计划编排、再评估入口和长期训练信任打地基。

产品与技术设计：领域层新增 `buildTargetedDrillTrend`，把最近的 adaptive / keyboard-zone 结果聚合成长期趋势，包括最新信号、降温比例、已清掉次数、仍需窄练次数和活跃问题列表；洞察页 model 把这些趋势翻译成用户文案；UI 新增“命中趋势”面板，展示最新信号、统计卡片和活跃问题状态列表。

改动范围：领域层专项趋势聚合、洞察页 model/UI、双语文案、样式、domain/web 测试、产品记录。

验收标准：

- 洞察页能显示最近专项训练的长期命中趋势，而不是只显示单轮反馈。
- 面板能区分“问题在降温 / 正在收敛 / 仍需窄练”三种状态。
- 能展示最近专项回合数、压力下降比例、已清掉次数和仍需窄练次数。
- 能展示当前仍在活跃的重点区域或训练焦点。
- 原有洞察页的会话趋势、技能画像、键位压力和历史列表不回退。

## 2026-06-09 键位专项进入训练计划

本轮诊断：洞察页已经能发现键位压力，也能一键启动对应专项，但这仍然像一次性的临时反应。对于连续几轮都在同一区域掉点的用户，系统还没有把这个信号升级成正式计划，导致“发现问题”和“持续修问题”之间仍然断开。

本轮需求：当同一键位压力在最近几轮里连续出现时，把它纳入 7 天起步计划，替换掉泛化的弱项练习步骤，让计划真正围绕用户的身体输入短板展开。

目标用户：已经完成评估、进入起步计划，且错误字符虽然分散但物理键位区域持续集中的用户，尤其是左手主键位、右手上排、数字排或符号层反复承压的练习者。

使用场景：用户完成评估后生成 7 天计划；如果系统发现同一区域在最近多轮里连续成为主要压力区，就会把 day 3 的“弱项补强”替换成对应键位专项步骤。用户从计划页、首页或结果页继续计划时，进入的会是真实的键位专项草稿，而不是普通内置练习。

预期收益：把键位专项从洞察页的“即时应对”提升为计划内的“持续修正”，让计划更贴近真实短板，也让专项训练、结果反馈和长期成长路径第一次串成一条线。

产品与技术设计：`buildSkillProfile` 新增连续键位压力识别，记录主要区域、区域占比、代表字符、连续出现次数和布局；`createStarterTrainingPlan` 在压力足够集中且连续时，把 day 3 改造成键位专项步骤；`createDraftFromTrainingStep` 支持从计划 step 直接生成 `keyboard-zone` 草稿，保留专项元数据，确保后续结果页命中反馈仍可工作。

改动范围：领域层能力画像、起步计划生成、训练 step 转草稿逻辑、训练流 use case、结果页专项反馈回归测试、产品记录。

验收标准：

- 能识别最近多轮里连续出现的同一区域键位压力。
- 7 天起步计划会在满足条件时把 day 3 替换成键位专项步骤。
- 从计划继续进入练习时，实际生成的是 `keyboard-zone` 草稿，而不是普通内置词库。
- 计划专项草稿保留键位区域、布局、代表字符和区域占比元数据。
- 原有评估流程、普通起步计划、结果页命中反馈和练习路径不回退。

## 2026-06-09 自适应训练后的命中反馈

本轮诊断：练习页已经能解释“为什么安排这轮自适应或键位专项”，但用户完成后回到结果页，看到的仍主要是总分、处方和教练建议。系统知道这轮原本瞄准了哪些问题，却没有告诉用户这些问题到底有没有真的降下来。

本轮需求：当用户完成自适应训练或键位专项后，在结果页给出命中反馈，直接回答“这轮是否打到了刚才要修的问题”，而不是只继续推荐下一轮。

目标用户：连续训练、愿意按建议再练一轮的用户，尤其是希望确认练习是否有效、避免“明明练了但不知道有没有进步”的进阶用户。

使用场景：用户通过结果页“再练一轮”进入自适应稿，或从洞察页进入键位专项稿；完成这一轮后回到结果页，页面除了通用处方，还展示本轮针对目标的命中反馈，包括目标错误是否减少、键位压力是否下降、哪些热点仍在出现。

预期收益：把“系统推荐了一轮训练”升级为“系统证明这轮训练有没有打到点上”，让用户更愿意继续跟随训练闭环，也为后续把专项问题纳入长期计划提供判断依据。

产品与技术设计：补充 session 结果里的 `errorCharStats` / `errorWordStats` 明细，让错误分布可以被重复计算；自适应草稿额外记录目标字符、目标单词、基线错误量和来源 session；领域层新增 `buildTargetedDrillFeedback`，为 adaptive / keyboard-zone 两类训练输出命中反馈摘要；结果页增加“命中反馈”面板，用统一文案展示收敛状态、跟踪错误、区域压力和剩余热点。

改动范围：领域层错误分布与反馈模型、练习草稿元数据、训练状态契约、结果页 model/UI、双语文案、样式、domain/contracts/web 测试。

验收标准：

- session 结果可保存字符级和单词级错误计数明细。
- 自适应草稿能记录目标热点、基线错误量和来源 session。
- 结果页对 adaptive / keyboard-zone 训练显示命中反馈面板。
- 命中反馈能区分“已经清掉”“正在收敛”“还需继续”三种状态。
- 原有结果页处方、挑战反馈、教练建议和趋势图不回退。

## 2026-06-09 键位专项自适应文本

本轮诊断：洞察页已经能识别主要键位压力区，但用户看到“左手 / 主键位”或“符号层”后，仍需要回到练习页手动调整模式或等待 AI。洞察和训练内容之间还差一个一键行动入口。

本轮需求：当洞察页发现主要键位压力区时，允许用户直接生成对应键位专项文本并进入练习，让下一轮围绕当前最集中的身体输入短板展开。

目标用户：已经积累多轮错误热点、想针对具体手位或键盘区域做短练习的用户，尤其是错误字符集中在主键位、右手上排、数字排或符号层的进阶练习者。

使用场景：用户进入洞察页查看“键位压力”，点击主要压力区的专项练习按钮；系统生成本地内置专项稿，保留键位区域、布局、代表字符和占比元数据，并直接跳转到练习页。

预期收益：把洞察页从“看见问题”推进到“立刻练这个问题”，减少用户自己翻译和配置训练的成本，也让后续命中反馈可以知道这轮训练原本瞄准的是哪个键位区域。

产品与技术设计：领域层新增 `createKeyboardZoneDrillDraft`，按压力区生成本地 words 模式草稿；数字排自动包含数字，符号层自动包含标点，普通手位优先混入代表错误字符和包含这些字符的常用词。训练状态契约扩展键位专项 source meta；Web store 增加 `setKeyboardZoneDrillDraft`，洞察页主压力区按钮调用后进入 `/practice`。

改动范围：领域层草稿生成、训练状态契约、练习草稿 use case、洞察页状态选择器与 model、洞察页 UI、双语文案、样式、domain/contracts/web 测试。

验收标准：

- 洞察页主压力区能一键生成本地键位专项草稿。
- 草稿元数据包含键位区域、键盘布局、代表字符和区域占比。
- 数字排专项开启数字训练，符号层专项开启标点训练。
- 启动专项练习会清理旧的训练上下文、AI 状态和练习错误。
- 点击后能跳转练习页，且练习 runtime 保存对应草稿与配置。

## 2026-06-09 错误热点键位维度

本轮诊断：洞察页已经能展示高频错误字符和单词，但用户看到 `a / s / d / j` 这类列表后，仍需要自己把字符翻译成“哪只手、哪一排、是否数字或符号层”的训练判断。对于连续训练用户来说，这会削弱洞察页的行动价值。

本轮需求：把最近错误字符聚合到键盘区域，帮助用户快速判断主要压力来自左手、右手、主键位、上下排、数字排还是符号层。

目标用户：已经积累多轮训练历史、想定位具体输入短板的用户，尤其是错误字符分散但键位区域集中的进阶练习者。

使用场景：用户进入洞察页查看最近表现；除了高频字符和单词，还能看到“键位压力”面板，明确主压力区、近期错误总量、各区域占比和代表字符。

预期收益：把错误热点从字符清单升级为可执行诊断，让下一轮练习目标更容易被身体感知，也为后续键位专项训练和自适应文本反馈打基础。

产品与技术设计：`buildInsights` 接收当前键盘布局并输出 `keyboardHotspots`；领域层按 QWERTY/Colemak/Dvorak 的物理键位区域聚合最近 30 轮错误字符；洞察页新增“键位压力”面板展示主压力区和区域占比；无错误样本时保留现有空状态。

改动范围：领域层洞察模型、洞察页状态选择器与 model、洞察页 UI、双语文案、响应式样式、domain/web 测试。

验收标准：

- 最近 30 轮错误字符能聚合为键盘区域。
- 聚合逻辑能识别数字排、符号层和不同键盘布局。
- 洞察页能展示主压力区、近期错误总量、代表字符和区域占比。
- 无错误样本时不制造空数据。
- 原有错误字符、错误单词、趋势和历史列表不回退。

## 2026-06-09 自适应训练可解释化

本轮诊断：上一轮已经让“再练一轮”能在 AI 不可用时生成本地自适应训练稿，但用户进入练习页时只能看到“自适应准确率训练”这类来源标签。对于真实用户来说，系统为什么选这个重点、为什么塞入这些词仍不够透明，容易把自适应训练看成普通换词。

本轮需求：让自适应训练稿携带可解释元数据，并在练习开始前用一条克制的说明告诉用户本轮训练依据。

目标用户：连续复盘后进入下一轮的用户，尤其是希望理解系统推荐逻辑、建立训练信任的新手和进阶用户。

使用场景：用户在结果页点击“再练一轮”，系统生成本地自适应稿后进入练习页；在尚未开始输入前，页面展示本轮焦点、触发信号和错误热点，输入开始后自动收起。

预期收益：把自适应训练从“系统替我换了一段文本”提升为“系统针对我的上一轮问题安排了一段练习”，增强训练闭环的可信度和可感知价值。

产品与技术设计：领域层 `createAdaptiveDrillDraft` 写入 `adaptiveFocus`、`adaptiveHotspots` 和 `adaptiveMetrics`；contracts 显式声明这些 source meta 字段；练习页 model 只在 adaptive draft 且 idle 状态生成 `adaptiveDrillInsight`；练习页用轻量面板展示焦点、触发信号和热点，避免干扰打字主区域。

改动范围：领域层草稿元数据、训练状态契约、练习页 model/UI、双语文案、样式、domain/contracts/web 渲染测试。

验收标准：

- 自适应草稿包含焦点、热点和关键指标元数据。
- contracts 能稳定解析自适应 source meta。
- 练习页在自适应稿开始前展示训练依据。
- 输入开始后解释面板不再出现，保持专注输入。
- 普通内置、AI、自定义和计划/挑战练习不展示该面板。

## 2026-06-09 处方驱动的自适应下一轮

本轮诊断：结果页和练习页已经能展示“下一轮处方”，但真正进入下一轮时，训练文本仍主要依赖普通内置词库或 AI 生成。AI 不可用时，用户看到的处方不会稳定落到练习内容上，导致“知道该练什么”和“实际练到什么”之间仍有断层。

本轮需求：把上一轮结果处方转化为本地可生成的自适应训练稿，让“再练一轮”在没有 AI 的情况下也能围绕准确率、节奏、返工或速度目标生成匹配文本。

目标用户：完成一轮后想马上继续训练的用户，尤其是没有配置 AI、网络不稳定或只想快速进入下一轮的用户。

使用场景：用户在结果页点击“再练一轮”；如果 AI 教练文本可用，仍优先使用 AI；如果 AI 生成失败或尚无教练下一轮，系统直接基于上一轮错误热点和指标生成一轮本地自适应短练习。

预期收益：把处方从提示升级为训练内容，降低 AI 依赖，保证核心训练闭环稳定可用，并让每一轮继续练习更像有目标的产品体验。

产品与技术设计：在 `packages/domain` 增加纯规则自适应草稿生成，按准确率低、节奏波动、原始速度与净速度差、稳定提速四类焦点选择文本结构；优先复用上一轮高频错词和错字映射词；Web store 在 `launchNextDrill` 中保留 AI 优先策略，并在失败或缺少下一轮建议时自动切换到本地自适应草稿。

改动范围：领域层草稿生成、练习草稿 use case、历史/教练 action 边界、结果页下一轮启动逻辑、领域测试和 store 测试。

验收标准：

- 准确率不足时生成“自适应准确率训练”，包含上一轮错误热点。
- 已经稳定的回合生成更密集的“自适应加速训练”。
- AI 下一轮生成失败时，结果页“再练一轮”仍能进入本地自适应练习。
- 没有教练下一轮时，结果页也能基于最近 session 生成自适应练习。
- 自适应草稿会清理 AI 错误状态，并保持普通练习路径可直接输入。

## 2026-06-09 练习前目标承接

本轮诊断：上一轮已经把结果页从“看分数”推进到“给出下一轮处方”，但用户点击继续后，练习页仍首先呈现配置和文本状态。结果页给出的重点、剂量、验收线没有在输入前继续出现，导致闭环在最关键的下一轮开始前断开。

本轮需求：把最近一次结果页处方带入下一轮练习页，在用户尚未开始输入时显示一个简洁的目标面板，让用户先看到本轮重点、练习剂量和验收线，再进入打字。

目标用户：完成一轮后立即继续练习的用户，尤其是需要明确“这轮到底练什么”的新手和习惯养成期用户。

使用场景：用户在结果页点击继续计划、再练一轮、重试挑战或自由练习后进入练习页；在开始输入前，页面顶部承接上一轮处方，输入开始后面板自动收起，让练习界面保持专注。

预期收益：减少从复盘到再练之间的认知丢失，把“建议”变成“开始前目标”，提升连续训练的确定性和用户对系统判断的信任。

产品与技术设计：复用 `buildResultPrescriptionModel` 作为唯一处方来源；练习页 store 暴露最近完成 session 和匹配的最近教练建议；练习页 model 在 idle 状态生成 `nextRoundBrief`；页面使用现有处方网格样式渲染目标面板，输入开始后隐藏，避免干扰核心输入。

改动范围：练习页状态选择器、练习页模型、练习页 UI、双语训练文案、处方样式复用和练习页渲染测试。

验收标准：

- 练习页在输入前能显示最近结果的下一轮处方。
- 处方内容与结果页使用同一套模型，包含重点、剂量、验收线。
- 若最近教练建议属于同一 session，练习页沿用其下一轮理由。
- 用户开始输入后目标面板自动隐藏，练习界面保持专注。
- 结果页原有处方和挑战/计划决策不回退。
- 已补充练习页渲染测试，并回归结果页相关测试。

## 2026-06-09 结果页下一轮处方

本轮诊断：TypeMaster 已经具备评估、计划、挑战、练习、复盘的主闭环，但结果页的用户价值仍偏向“看分数和建议”。当 AI 建议加载中、失败或信息较长时，用户缺少一个足够短、可立即执行的下一轮目标。

本轮需求：在结果页新增确定性的下一轮练习处方，把本轮表现转化为“重点、剂量、验收线”三项可执行信息。

目标用户：完成一轮练习后想马上继续进步的打字训练用户，尤其是还没有形成稳定训练习惯的新用户。

使用场景：用户结束练习后进入结果页，不需要重新选择模式，也不必等待 AI 建议，就能知道下一轮该保护准确率、抹平节奏、减少返工，还是小幅加速。

预期收益：降低复盘后的决策成本，让练习闭环从“完成一轮”自然推进到“带目标再练一轮”，提升连续训练率和产品可信度。

改动范围：结果页模型、结果页 UI、双语文案、响应式样式和结果页相关测试。

验收标准：

- 结果页始终显示下一轮处方。
- 处方基于准确率、稳定性、原始速度与净速度差等本轮指标确定重点。
- 处方包含重点、练习剂量和验收线三项信息。
- AI 建议不可用时处方仍可工作。
- 桌面和移动端布局不挤压现有结果页操作。
- 已补充模型测试和页面渲染测试。

## 下一轮高价值方向

1. 挑战与计划的节奏保护：当挑战连续拉低准确率时，在首页和挑战页主动建议退出排名压力。
2. 数据导入后的恢复路径：导入历史数据后自动生成当前状态摘要，帮助跨设备用户快速续接训练。
3. 计划专项的节奏编排：当计划里已经出现键位专项时，自动安排前后承接的恢复轮和验收轮，避免用户在局部压力里练得过窄。
4. 洞察页的训练建议回流：首页和洞察页根据“命中趋势”主动提示继续窄练、回到计划，还是切回自由练习。
5. 再评估后的阶段差异说明：当用户完成第二次及之后的评估时，明确告诉他新计划相对上一个阶段发生了什么变化。
