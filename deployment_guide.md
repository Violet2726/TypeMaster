# 部署指南：如何让其他人使用你的 TypeMaster

这里有三种方法可以让其他人访问你的项目，取决于你的需求（是给局域网同事用、临时给朋友看，还是永久发布）。

> [!WARNING]
> **⚠️极其重要的安全提示：API Key 泄露风险**
> 目前你的 `AI_API_KEY` 是直接写在 `app.js` 里的。
> *   **局域网/个人使用**：风险较低，但仍需小心。
> *   **发布到公网 (Vercel/GitHub Pages)**：**非常危险！** 任何人查看网页源代码都能看到并盗用你的 Key。
> *   **建议**：如果是公网发布，建议配合后端服务转发请求，或使用 Vercel 的 Serverless Function 来隐藏 Key。

---

## 方法一：局域网共享 (为你定制的简易模式)
**适用场景**：其他人都在同一个 WiFi 下（如办公室、宿舍）。

由于你的电脑上安装 `http-server` 遇到权限问题，我为你创建了一个专属的启动脚本 `server.js`。

1.  **启动服务**
    此项目目录下已为你创建了 `server.js`。直接在终端运行：
    ```bash
    node server.js
    ```
    *(看到 `Server running at...` 即表示启动成功)*

2.  **分享链接**
    根据你的网络环境，你的局域网 IP 很可能是：
    > **http://192.168.1.4:8080**

    或者
    > **http://192.168.253.1:8080**
    
    让朋友连接同一个 WiFi，并在浏览器输入上面的地址即可！

---

## 方法二：内网穿透 (临时公网访问)
**适用场景**：朋友不在同一个 WiFi，但你不想麻烦地去部署服务器。需要电脑开着。

可以使用工具如 **cpolar** 或 **ngrok**。

1.  下载并安装 [cpolar](https://www.cpolar.com/) (国内速度较快)。
2.  先执行方法一启动本地服务 (端口 8080)。
3.  运行 cpolar 映射端口：
    ```bash
    cpolar http 8080
    ```
4.  cpolar 会生成一个公网链接 (如 `http://Example.cpolar.cn`)，直接发给朋友即可访问。

---

## 方法三：云端托管 (Vercel/Netlify)
**适用场景**：永久在线，发给任何人随时都能用。

**推荐使用 Vercel (操作最简单)**：

1.  **注册账号**：去 [vercel.com](https://vercel.com/) 注册。
2.  **安装 Vercel CLI** (在终端运行)：
    ```bash
    npm i -g vercel
    ```
3.  **部署**：
    在项目目录下运行：
    ```bash
    vercel
    ```
    *   一路按回车 (Enter) 确认默认设置。
4.  **完成**：
    Vercel 会给你一个类似 `https://typing-project-five.vercel.app` 的网址。

**再次提醒**：在这个纯前端版本中部署到云端，请务必注意 API Key 的额度消耗，防止被盗刷。为了安全，可以在 API 提供商后台设置 **额度上限** 或 **绑定域名白名单**。
