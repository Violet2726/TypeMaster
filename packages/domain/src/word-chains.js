/** * Word Chain System * * When players type consecutive related words, they trigger Chain Attacks - * area-of-effect damage that hits multiple enemies at once. */export const WORD_CHAINS = [  { words: ["attack", "defend"], bonus: 2, label: "Combat Chain", labelZh: "战斗连携", color: "#ef4444" },  { words: ["fire", "water"], bonus: 2, label: "Elemental Chain", labelZh: "元素连携", color: "#3b82f6" },  { words: ["light", "dark"], bonus: 2, label: "Light and Dark", labelZh: "光暗连携", color: "#a855f7" },  { words: ["attack", "power"], bonus: 3, label: "Power Strike", labelZh: "力量打击", color: "#f97316" },  { words: ["speed", "force"], bonus: 3, label: "Speed Force", labelZh: "速度之力", color: "#06b6d4" },  { words: ["code", "debug"], bonus: 2, label: "Dev Chain", labelZh: "开发连携", color: "#22c55e" },  { words: ["data", "cache"], bonus: 2, label: "Data Chain", labelZh: "数据连携", color: "#8b5cf6" },  { words: ["byte", "code"], bonus: 2, label: "Byte Chain", labelZh: "字节连携", color: "#14b8a6" },  { words: ["loop", "break"], bonus: 3, label: "Loop Break", labelZh: "循环突破", color: "#f59e0b" },  { words: ["null", "void"], bonus: 2, label: "Void Chain", labelZh: "虚空连携", color: "#6366f1" },  { words: ["sun", "moon"], bonus: 2, label: "Celestial", labelZh: "天体连携", color: "#fbbf24" },  { words: ["fire", "ice"], bonus: 2, label: "Temperature", labelZh: "温度连携", color: "#06b6d4" },  { words: ["earth", "sky"], bonus: 2, label: "World Chain", labelZh: "天地连携", color: "#22c55e" },  { words: ["wave", "wind"], bonus: 2, label: "Nature Chain", labelZh: "自然连携", color: "#10b981" },  { words: ["jump", "run"], bonus: 2, label: "Motion Chain", labelZh: "运动连携", color: "#ec4899" },  { words: ["push", "pull"], bonus: 2, label: "Force Chain", labelZh: "力量连携", color: "#f43f5e" },  { words: ["start", "stop"], bonus: 2, label: "Control Chain", labelZh: "控制连携", color: "#8b5cf6" },  { words: ["open", "close"], bonus: 2, label: "Toggle Chain", labelZh: "切换连携", color: "#06b6d4" },];export function findChainMatch(completedWords, chainLength) {  chainLength = chainLength || 2;  const recent = completedWords.slice(-chainLength);  if (recent.length < chainLength) return null;  for (const chain of WORD_CHAINS) {    for (let i = 0; i <= recent.length - chainLength; i++) {      const slice = recent.slice(i, i + chainLength);      if (chain.words.every(function(w, idx) { return w.toLowerCase() === (slice[idx] || "").toLowerCase(); })) return chain;    }    var reversed = chain.words.slice().reverse();    for (let i = 0; i <= recent.length - chainLength; i++) {      const slice = recent.slice(i, i + chainLength);      if (reversed.every(function(w, idx) { return w.toLowerCase() === (slice[idx] || "").toLowerCase(); })) return chain;    }  }  return null;}export function calculateChainScore(chain, combo) {  return chain.bonus * 100 * Math.max(1, Math.floor(combo / 5));}export function getChainHint(completedWords) {  for (const chain of WORD_CHAINS) {    var recent = completedWords.slice(-1);    if (recent.length === 0) continue;    var lastWord = recent[0].toLowerCase();    var wordIndex = -1;    for (var j = 0; j < chain.words.length; j++) {      if (chain.words[j].toLowerCase() === lastWord) { wordIndex = j; break; }    }    if (wordIndex >= 0 && wordIndex < chain.words.length - 1) {      return { chain: chain, nextWord: chain.words[wordIndex + 1], progress: wordIndex + 1, total: chain.words.length };    }  }  return null;}