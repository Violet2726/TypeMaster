/** * Rhythm Reward Visual * * Shows the player their rhythm state and damage multiplier. * Apple-style: minimal, informative, beautiful. */export function renderRhythmReward(    ctx: CanvasRenderingContext2D,    w: number,    h: number,    rhythmScore: number,    damageMultiplier: number,    streak: number,    time: number,): void {    if (rhythmScore < 0.3 && streak < 3) return;    const x = 20;    const y = h - 40;    ctx.save();    const barW = 80;    const barH = 3;    const barX = x;    const barY = y;    ctx.fillStyle = "rgba(255,255,255,0.1)";    ctx.beginPath();    ctx.roundRect(barX, barY, barW, barH, 1.5);    ctx.fill();    let barColor = "#3b82f6";    if (rhythmScore > 0.9) barColor = "#ffd700";    else if (rhythmScore > 0.8) barColor = "#22c55e";    else if (rhythmScore > 0.6) barColor = "#06b6d4";    ctx.fillStyle = barColor;    ctx.beginPath();    ctx.roundRect(barX, barY, barW * rhythmScore, barH, 1.5);    ctx.fill();    if (rhythmScore > 0.8) {        ctx.shadowColor = barColor;        ctx.shadowBlur = 8;        ctx.fillStyle = barColor + "40";        ctx.beginPath();        ctx.roundRect(barX, barY - 2, barW * rhythmScore, barH + 4, 3);        ctx.fill();        ctx.shadowBlur = 0;    }    if (damageMultiplier > 1.0) {        ctx.font = "600 10px -apple-system, SF Pro Text, system-ui, sans-serif";        ctx.fillStyle = barColor;        ctx.textAlign = "left";        ctx.textBaseline = "top";        const pulse = 1 + Math.sin(time * 0.005) * 0.05;        ctx.save();        ctx.translate(barX + barW + 8, barY - 5);        ctx.scale(pulse, pulse);        ctx.fillText("x" + damageMultiplier.toFixed(1), 0, 0);        ctx.restore();    }    if (streak >= 5) {        const streakY = y + 12;        ctx.font = "500 9px -apple-system, SF Pro Text, system-ui, sans-serif";        ctx.fillStyle = "rgba(255,255,255,0.5)";        ctx.textAlign = "left";        ctx.textBaseline = "top";        ctx.fillText(streak + " streak", barX, streakY);    }    ctx.restore();}