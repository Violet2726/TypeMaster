export function buildAchievementDomain({
    achievements,
    sessionStreak,
    weeklySessions,
    weeklyGoal
}) {
    return {
        achievements,
        sessionStreak,
        weeklySessions,
        weeklyGoal
    };
}
