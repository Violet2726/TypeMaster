export function buildConfigDomain({
    settings,
    updateSettings,
    config,
    updateConfig,
    applyCustomWordBank
}) {
    return {
        settings,
        updateSettings,
        config,
        updateConfig,
        applyCustomWordBank
    };
}
